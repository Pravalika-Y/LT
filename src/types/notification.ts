export interface Notification {
  id: string;
  senderName: string;
  senderRole: string;
  receiverName: string;
  receiverRole: string;
  message: string;
  timestamp: string;
  courseCode: string;
  initials: string;
  bgColor: string;
}
